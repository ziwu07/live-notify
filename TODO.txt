add msg to expect
interval in config
add delay between each notifications
set time ranges in local time where:
  no notifications? (still open_direct)
  no audiable notifications? (still dbus notify but with no sound)
  nothing at all?
action_invoked is getting called by notifications that did not originate from this program
