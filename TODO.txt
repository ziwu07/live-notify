add msg to expect
set time ranges in local time where:
  no notifications? (still open_direct) open_direct_only
  no audiable notifications? (still dbus notify but with no sound) silent
  nothing at all? pause
size check for get_file_path
